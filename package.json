{
  "name": "laravel-learn-skills",
  "private": true,
  "type": "module",
  "description": "Agent skills to learn Laravel by building a real app, with an AI as teacher and reviewer.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "sync": "node scripts/sync-shared.mjs",
    "validate": "node scripts/validate.mjs",
    "install-local": "node scripts/install-local.mjs"
  }
}
